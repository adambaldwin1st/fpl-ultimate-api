name: Build and Deploy API (Stage)

on:
  push:
    branches: [main]

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Run tests
        run: mvn -B test
        env:
          FOOTBALL_API_USERNAME: test
          FOOTBALL_API_PASSWORD: test
          FOOTBALL_API_KEY: test

      - name: Build jar
        run: mvn -B -DskipTests package

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ghcr.io/${{ github.repository }}:stage

  deploy:
    needs: build-and-push
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Pull and restart staging container
        run: |
          docker compose --env-file /opt/fpl-ultimate/.env pull api-stage
          docker compose --env-file /opt/fpl-ultimate/.env up -d api-stage

      - name: Smoke test
        run: |
          sleep 5
          curl -sf http://localhost:5002/health
