# No third-party dependencies yet — app.py uses only the Python standard library.
# Add packages here if the scraper grows (e.g. requests, pydantic) and re-run the
# packaging step in lambda/README.md before deploying.
